---
name: meeting-room-booking-reviewer
description: Review meeting room booking requests and classify them as 승인, 조정요청, or 반려 based on business-hour compliance, duplicate booking conflicts, and room capacity. Use this skill whenever the user asks to review, approve, reject, triage, or check a meeting room reservation request, reservation overlap, booking availability, or rescheduling need for a meeting room.
---

# Meeting Room Booking Reviewer

회의실 예약 요청을 검토해 `승인`, `조정요청`, `반려` 중 하나로 판정한다.

## 언제 이 스킬을 사용할지

다음과 같은 요청에서 이 스킬을 사용한다.
- 회의실 예약 요청 검토
- 예약 승인 가능 여부 판단
- 중복 예약 확인
- 업무시간 준수 여부 점검
- 참석 인원과 정원 적합성 검토
- 대체 시간 또는 보완 필요사항 정리

## 필요한 입력

가능하면 아래 정보를 확보한다.
- 요청 회의실명
- 예약 날짜
- 시작 시각
- 종료 시각
- 참석 인원 수
- 회의실 정원
- 기존 예약 목록(없으면 없다고 명시)

기존 예약 목록은 각 예약의 시작/종료 시각이 포함되어야 한다.

## 판정 규칙

규칙은 아래 순서로 적용한다.

1. **동일 시간대 중복 예약 확인**
   - 같은 회의실에 기존 예약과 시간이 겹치면 `반려`로 판정한다.
   - 시간 겹침은 요청 시작시각 < 기존 종료시각 이고 요청 종료시각 > 기존 시작시각 이면 성립한다.

2. **업무시간 준수 확인**
   - 업무시간은 `09:00~18:00` 이다.
   - 시작시각이 09:00 이전이거나 종료시각이 18:00 이후이면 `조정요청`으로 판정한다.

3. **정원 초과 확인**
   - 참석 인원이 회의실 정원을 초과하면 `조정요청`으로 판정한다.

## 우선순위

여러 규칙이 동시에 걸리면 가장 강한 판정을 우선한다.
- `반려` > `조정요청` > `승인`

즉,
- 중복 예약이 있으면 다른 조건과 무관하게 `반려`
- 중복 예약은 없지만 업무시간 위반 또는 정원 초과가 있으면 `조정요청`
- 위 조건이 모두 없으면 `승인`

## 출력 형식

항상 아래 구조로 간결하게 답한다.

### 1) 판정
- `승인` / `조정요청` / `반려` 중 하나

### 2) 사유
- 규칙에 근거한 핵심 사유를 불릿으로 정리

### 3) 조치 제안
- `승인`: 예약 진행 가능하다고 적기
- `조정요청`: 조정이 필요한 항목(시간 조정, 더 큰 회의실 검토 등) 제안
- `반려`: 겹치는 기존 예약 때문에 해당 시간대 예약 불가라고 적고, 가능하면 대체 시간 확인 필요 안내

## 작성 원칙

- 판정 사유는 한 문장으로 간결하게 쓴다.
- 규칙에 없는 임의 기준을 추가하지 않는다.
- 사용자가 제공한 정보만 사용한다.
- 정보가 부족해도 가능한 범위에서 판단하되, 누락 정보가 판정에 영향을 주면 그 점을 명시한다.
- 최종 문장에는 판정 결과가 분명히 드러나야 한다.

## 예시

**입력 예시**
- 회의실: A회의실
- 시간: 2025-09-10 10:00~11:00
- 참석: 6명
- 정원: 8명
- 기존 예약: 없음

**출력 예시**
- 판정: 승인
- 사유:
  - 업무시간 내 예약입니다.
  - 중복 예약이 없습니다.
  - 참석 인원이 정원 이내입니다.
- 조치 제안:
  - 예약 진행 가능합니다.
