---
name: meeting-room-booking-reviewer
description: 회의실 예약 요청을 검토할 때, 반드시 동일 회의실/자원 중복 탐색을 1순위로 수행하고 결과를 지정된 6개 컬럼의 표로만 반환한다. 조직 표준 시간대(예: Asia/Seoul)로 정규화, 버퍼/반복 예약/경계 접합/올데이 처리, 정책 상태 필터(취소/거절 제외), 예외 처리(재시도, 누락 데이터) 및 검증 절차를 포함한다. 이 스킬은 실제 캘린더/자원 DB 조회 도구 사용 및 조회 로그 확보 없이는 제출을 금지한다.
---

# Meeting Room Booking Reviewer (중복 우선·표준 서식 고정)

회의실/자원 예약 요청을 검토할 때, 항상 "동일 회의실/자원 중복 탐색"을 최우선으로 수행하고, 결과는 지정된 6개 컬럼의 표 형식으로만 반환한다(줄글 금지). 도구로 실제 캘린더/자원 DB를 조회하고 조회 로그를 확보하지 못하면 제출하지 않는다.

## 적용 범위
- 회의실 또는 예약형 자원(프로젝터, 스튜디오 등)의 예약 가능성/중복 여부/조치 제안 검토
- 단일 또는 복수 요청 배치 처리(여러 요청은 표 행 단위로 누적)

## 필수 파라미터(입력 정규화)
- 요청자(requester)
- 요청 ID(request_id)
- 회의실/자원 ID(resource_id), 이름(resource_name)
- 시작 시각(start), 종료 시각(end)
- 조직 표준 시간대(TZ): 기본 Asia/Seoul
- 형식: YYYY-MM-DD HH:MM~HH:MM (24시간제, 현지 TZ)
- 정책 옵션(있다면):
  - 버퍼(buffer_minutes, 기본 0)
  - 포함 상태(status_filter, 예: ["확정","대기"]) — 취소/거절은 제외
  - 우선순위 규칙(priority_rules): 선접수/승인상태/호스트 직급 등

정규화 규칙
- 입력 시각을 조직 표준 TZ로 변환. 올데이/종일은 해당 날짜 00:00~24:00으로 확장.
- 종료=시작(0분 길이)은 유효하지만 중복 비교시 경계 접합 조건에 유의.

## 표준 절차(수정 반영)
1) 입력 정규화
- 필수 필드 수집: 요청자, 요청 ID, 자원 ID/이름, 시작·종료시각
- 모두 조직 표준 TZ(예: Asia/Seoul)로 변환, 포맷 YYYY-MM-DD HH:MM~HH:MM

2) 중복 탐색(최우선)
- 대상: 동일 회의실/자원에 한정
- 기간: [요청 시작, 요청 종료) 기준. 완전/부분 겹침 모두 탐지
- 버퍼: 정책이 있으면 전후 N분 포함해 비교
- 반복 예약: 요청과 기존 항목 모두 인스턴스로 전개한 뒤 해당 일자 범위 내 비교
- 조회: 캘린더/자원 DB에서 정책상 "활성"으로 간주되는 상태만 포함(예: 확정/대기), 취소/거절 제외
- 경계 접합: 종료=시작(또는 시작=종료)은 중복 아님

3) 판정 및 식별
- 중복 여부: 예/아니오 결정
- 충돌 항목: ID와 접근 링크 수집(여러 건은 쉼표 구분). 링크가 없으면 ID만, 없을 때는 없음

4) 조치 결정(비고)
- 중복=예: 보류 또는 반려 표기, 대안 제시(시간 이동/대체 회의실). 우선순위 규칙(선접수/승인상태/호스트 직급 등)이 있으면 자동 결론 표기
- 중복=아니오: 승인 가능 표기(필요 시 업무시간·정원 등 추가 정책을 검토해 조정 제안 가능)

5) 산출물 작성(서식 서브루틴, 표만)
- 6개 컬럼만 포함: 요청자 | 일정(일시) | 회의실/자원 | 중복 여부 | 중복 항목 식별자/링크 | 비고(조치)
- 여러 요청이면 행 단위로 누적
- 정렬: 기본 요청 접수시각 오름차순
- 시간 표기: 24시간제, 현지 TZ
- 줄글/불릿 등 표 외 서술 금지

6) 검증
- 필수 컬럼 누락/빈값 점검
- 줄글 서술 포함 여부 검사(헤더/행 외 텍스트 금지)
- 겹침 로직 단위 테스트(완전/부분/경계/반복/버퍼/올데이/취소제외) 통과 확인
- 조회 수행 로그 확인(엔드포인트/파라미터/시각/요청 ID/재시도 횟수). 로그 없으면 제출 금지

## 중복 판정 기준(명시)
- Overlap 조건: req_start < exist_end AND req_end > exist_start
- 경계: req_end == exist_start 또는 req_start == exist_end → 비중복
- 올데이/종일: 해당 날짜의 00:00~24:00로 확장 비교
- 상태: 취소/거절 제외, 정책상 활성(확정/대기 등)만 포함
- 버퍼: 비교 시 요청/기존 양측에 버퍼 확장 적용(정책에 따름)

의사코드
- normalize(req)
- window_req = expand_recurrence(req) and apply_buffer(req)
- events = fetch_active_events(resource_id, date_range)
- events_inst = expand_recurrence(events) and apply_buffer(events)
- overlap = any(r.start < e.end && r.end > e.start for r in window_req for e in events_inst)

## 예외 처리
- 데이터 누락: 누락 필드 표시 및 검토 보류로 표기, 원요청자/관리자에 보완 요청
- API 오류: 지수 백오프 최대 3회 재시도(예: 1s, 2s, 4s). 모두 실패 시 비고에 "조회 실패, 재시도 예정" 표기
- 다중 충돌: 모든 충돌 ID/링크를 나열, 우선순위 규칙으로 1차 결론 제시

## 출력 템플릿(예시)
요청자 | 일정(일시) | 회의실/자원 | 중복 여부 | 중복 항목 식별자/링크 | 비고(조치)
홍길동 | 2026-09-30 10:00~11:00 | FBT-301 | 예 | RES-12345(https://.../RES-12345) | 보류, 11:00~12:00 제안

표기 원칙
- 중복 여부: "예" 또는 "아니오"(조직 정책에 따라 Y/N 사용 가능)
- 링크가 없으면 ID만 표기, 모두 없으면 "없음"

## 추가 정책 검토(선택 — 비고 칸에 반영)
- 업무시간 준수(예: 09:00~18:00)
- 정원/수용 인원 적합성
- 장비/설비 요구사항 충족 여부
- 비고(조치)에 구체적 제안(시간 이동, 대체 회의실, 참석자 수 조정 등)을 간결히 기입

## 도구 사용 및 로그 의무
- 반드시 캘린더/자원 DB 조회 도구를 호출해 현황을 가져온다
- 로그에 포함: 호출 시각, 엔드포인트, 파라미터(resource_id, date_range, status_filter 등), 요청 ID, 재시도 횟수/결과
- 로그 미확보 시 산출물 제출 금지

## 단위 테스트 케이스 가이드(요약)
- 완전 겹침: 10:00~11:00 vs 10:00~11:00 → 예
- 부분 겹침: 10:00~11:00 vs 10:30~11:30 → 예
- 경계 접합: 10:00~11:00 vs 11:00~12:00 → 아니오
- 내부 포함: 10:00~12:00 vs 10:30~11:00 → 예
- 올데이 포함: 00:00~24:00 vs 09:00~10:00 → 예
- 버퍼 적용: 버퍼 10분에서 10:00~11:00 vs 09:50~10:00 → 예
- 취소/거절 제외: 상태가 취소면 비교 제외 → 아니오
- 반복 예약: 매주 화 10:00~11:00 vs 2026-09-29 10:30~11:30(화) → 예

## 품질 체크리스트(제출 전)
- [ ] 도구 조회 수행 및 로그 확보 완료
- [ ] 6개 컬럼만 존재(헤더 포함), 줄글 없음
- [ ] 시간/TZ/형식 규칙 준수, 정렬 규칙 준수
- [ ] 중복 여부/충돌 식별자 기입
- [ ] 예외/재시도 처리 결과 비고에 반영
